import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    map: {
        width: "100%",
        height: "100%",
    },
    container: {
        width: "80%",
        height: 400,
        borderRadius: 10,
        alignSelf: "center",
        marginTop: 30,
        overflow: "hidden"
    }
});

export default style;